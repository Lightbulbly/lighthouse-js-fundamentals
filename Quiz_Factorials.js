// var solution = 1;
// for (var x=1;  x < 12 ; x++)  
// { solution = solution* (x + 1); }
// console.log(solution);
// won't work becausse var cannot be usd in for?

let solution = 1;
for (let i=1;  i <= 12 ; i++)  
{ solution *= i; }
console.log(solution);
 